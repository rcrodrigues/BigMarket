#modalLogin.modal.fade(data-backdrop='static', data-ng-controller='headerController')
	.modal-dialog
		.modal-content

			.modal-header
				
				h4.modal-title Acesso ao Sistema de Vendas
					i.material-icons.pull-right(data-dismiss="modal") close

			.modal-body

				.form
					.form-group
						label(for="login") Login:
						input.form-control(type="text", size="15", name="login", ng-model="userLogin", autofocus)

					.form-group
						label(for="senha") Senha:
						input.form-control(type="password", size="15", name="senha", ng-model="senha")

			.modal-footer

				a.text-center(style='margin-right:5px', href='#/cadastrarUsuario').
					Ser um vendedor!
				a.btn.btn-success(type="button", ng-click="login()")
					i.material-icons check
					span  Entrar
				